code-along only works for synchronous scripts.

it also has basic infinite loop detection that throws an error for iterations over 1000 steps

to use the test.html file, first move it up one directory.  paths are configured for this
